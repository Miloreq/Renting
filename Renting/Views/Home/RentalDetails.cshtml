@model Renting.Models.Rental

@{
    ViewData["Title"] = "Szczegó³y wypo¿yczenia";
}

<div class="container w-75 mx-auto mt-4">
    <h2>Szczegó³y wypo¿yczenia</h2>
    <hr />
    <dl class="row">
        <dt class="col-sm-3">Status</dt>
        <dd class="col-sm-9">@Model.Status</dd>

        <dt class="col-sm-3">Od</dt>
        <dd class="col-sm-9">@Model.FromDate.ToString("yyyy-MM-dd")</dd>

        <dt class="col-sm-3">Do</dt>
        <dd class="col-sm-9">@Model.ToDate.ToString("yyyy-MM-dd")</dd>

        <dt class="col-sm-3">Przedmiot</dt>
        <dd class="col-sm-9">@Model.Assets?.Name</dd>

        <dt class="col-sm-3">U¿ytkownik</dt>
        <dd class="col-sm-9">@Model.User?.FullName</dd>

        <dt class="col-sm-3">Email</dt>
        <dd class="col-sm-9">@Model.User?.UserName</dd>

        <dt class="col-sm-3">Notatki</dt>
        <dd class="col-sm-9">@Model.Notes</dd>

        <dt class="col-sm-3">Stan po zwrocie</dt>
        <dd class="col-sm-9">@Model.Condition</dd>

        <dt class="col-sm-3">Notatki o uszkodzeniach</dt>
        <dd class="col-sm-9">@Model.DamageNotes</dd>

        <dt class="col-sm-3">Data wypo¿yczenia</dt>
        <dd class="col-sm-9">@Model.CheckedOutAt?.ToString("yyyy-MM-dd HH:mm")</dd>

        <dt class="col-sm-3">Data zwrotu</dt>
        <dd class="col-sm-9">@Model.ReturnedAt?.ToString("yyyy-MM-dd HH:mm")</dd>
    </dl>
    <a asp-action="AdminRentals" class="btn btn-secondary">Powrót</a>
</div>